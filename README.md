# URL Shortener

A simple Spring Boot application to shorten URLs.

## Features
- Shorten long URLs
- Redirect using short codes
- View recent shortened links

## Tech Stack
- Java
- Spring Boot
- HTML, CSS, JavaScript

## Usage
1. Run the application
2. Use `/api/shorten` endpoint to shorten URLs
3. Access shortened links via `/r/{code}`
